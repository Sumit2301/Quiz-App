{
    "version": 2,
    "builds": [
      {
        "src": "quiz_app/wsgi.py",
        "use": "@vercel/python"
      }
    ],
    "env": {
      "DJANGO_SECRET_KEY": "your-secret-key",
      "DATABASE_URL": "mysql://root:root@localhost:3306/quiz_app"
    },
    "routes": [
      {
        "src": "/(.*)",
        "dest": "quiz_app.wsgi"
      }
    ]
  }
  